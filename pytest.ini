[pytest]
testpaths = tests
addopts = -q
markers =
    hardware: requires Raspberry Pi hardware or real camera/microphone
    supabase: requires live Supabase credentials and network
    slow: downloads or loads large AI models
